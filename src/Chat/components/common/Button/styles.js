import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textStyle: {
    color: 'white',
    padding: 10,
    fontSize: 18
  },

  primaryButtonStyle: {
    backgroundColor: '#FF1D48',
    borderRadius: 5,
    alignItems: 'center',
    marginTop: 10,
    marginBottom: 20
  }
});
